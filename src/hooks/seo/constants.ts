
import { KeywordData, PageData } from './types';

// Remove all dummy data exports and replace with empty arrays
export const initialKeywords: KeywordData[] = [];

export const initialPages: PageData[] = [];
